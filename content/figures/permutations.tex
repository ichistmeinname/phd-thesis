\begin{tikzpicture}
  \begin{semilogyaxis}[
    tickwidth = 0pt,
    xmajorgrids=true,
    grid style={dashed},
    height=6cm,
    width=14cm,
    bar width=0.2cm,
    ybar=0pt,
    xlabel = {\footnotesize{length of list}},
    ylabel = {\footnotesize{number of non-deterministic branches spawned}},
    ymax = 10000,
    xmax = 8,
    xmin = 0.,
    axis y line = left,
%    x tick label as interval,
    xticklabel style = { font = \footnotesize },
    xtick = {0.5,1.5,...,7.5},
    ytick = {1, 2, 6, 24, 120, 720, 5040, 
    xticklabels = {1,2,3,4,5,6,7},
    x tick label as interval,
    axis x line* = bottom,
    xlabel style={at={(0.5,-0.125)}},
    enlarge y limits  = 0.02,
    legend style={
      at={(0.5,-0.275)},
      anchor=north,
      legend columns=-1,
      /tikz/every even column/.append style={column sep=0.5cm}}
  ]

  %Curry
  \addplot+[curry]
    coordinates {
                (1,1)
                (2,2)
                (3,4)
                (4,8)
                (5,16)
                (6,32)
                (7,64)
    };

  % Haskell
  \addplot+[haskell]
    coordinates {
                (1,1)
                (2,2)
                (3,6)
                (4,24)
                (5,120)
                (6,720)
                (7,5040)
    };

  \legend{Curry, Haskell}
  \end{semilogyaxis}

\end{tikzpicture}